/**
 * Deploy trên subdomain riêng cùng gốc domain với app chính (vd
 * zalo-forward.timetech.markeeai.com), publish thẳng port 3003 ra host —
 * KHÔNG dùng basePath/path-prefix (khác thiết kế ban đầu).
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
